#contains definitions of the dimensions of the space worked over

# x is the spacial labels of the model
label_x = [ x for x in range(2) ]
# t is the timestep labels of the model
label_t = [ t for t in range(6) ]
# i is the timestep labels of the observations
label_i = [ i for i in range(1, 5) ]
# deltat is the size of one timestep
deltat = 1.0
